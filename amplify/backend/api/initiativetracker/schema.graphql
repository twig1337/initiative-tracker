type ContactUsSubmission
    @model
    @auth(rules: [{ allow: public, operations: [create], provider: iam }, { allow: owner }]) {
    id: ID!
    type: String!
    content: String!
    email: AWSEmail
}
